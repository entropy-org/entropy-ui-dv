# Support policy

The initial supported runtime is React 18.3 or React 19 with TypeScript, Vite
or Next.js, and current evergreen desktop browsers. SSR imports must be safe;
layout-dependent behavior starts after mount.

React Native, legacy browsers, non-React frameworks, and arbitrary deep imports
are outside the v1 support boundary.
